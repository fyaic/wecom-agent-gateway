# ADR 0001：只使用企业微信 Bot 身份

- 状态：已接受
- 日期：2026-08-20

## 决策

所有 IM 收发均使用同一个企业微信 Bot 身份。不引入真人账号代发，也不在 Bot 与企业应用/真人身份之间回退。

## 原因

用户对单一身份有明确产品要求。身份切换会造成权限、审计、会话归属和用户预期混乱，也会把 `wecom-cli` 的真人授权误当成 Channel 身份。

## 影响

Bot API 无法完成的消息动作必须显式失败或降级为 Bot 支持的形式；不能悄悄换身份完成。`wecom-cli` 仍可作为办公工具，但其结果由 Bot 回复。
