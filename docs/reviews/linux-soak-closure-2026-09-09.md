# Linux 24h 观察收尾：保留失败报告，独立补证

日期：2026-09-09。范围是维护者专属 Linux VM 的低负载、有限采样观察，不是生产容量、
物理宿主断网、跨主机 active-active 或全 Kernel 认证。主 Agent 执行实际收尾；Linux sub-agent 独立复核判据。

## 原自动报告：未通过

[原样 JSON](evidence/linux-systemd-soak-v3-20260908.json) 保留 `schemaVersion=3`、
`certifying=true`、**`passed=false`**。未修改判定、拼接窗口或用事后消息补入历史窗口。

- 报告 SHA256：`89881dcdd5b0fe0e56a46941f3965f6ec40ff82136dff334a8b1a7c0cf6e0aeb`。
- 采集器 SHA256：`11646c6113d0fdecf3da91045f64f2b2747f23a891a19855f8cae7bddb7c98c7`。
- 窗口：2026-09-08T05:03:11.375Z → 2026-09-09T05:03:11.416Z。
- 经过 86,400,041ms，2,877 次采样，最大间隔 30,107ms（门限 40,000ms）。
- 服务 inactive、PID/Invocation 变化、重启差值、live/ready/metrics 失败和资源探针失败均为 0。
- pending/leased 峰值以及最终 pending/leased/dead/spool 均为 0；磁盘低水位 7,381,970,944 bytes。
- 唯一失败门为 `journalEvidencePresent`：采集器只查询窗口开始后的日志，而静默服务没有新日志。
  采集 unit 因失败门退出为非零；不是 Gateway 进程退出。

报告只有聚合结果，不含完整原始样本。全零积压不代表窗口内持续处理真实用户流量。

## 独立补证与有限结论

在停止服务之前，另外读取 systemd 和 journal 元数据；不读取消息正文或公开原始身份标识。

| 证据                      | 实际结果                                                                                   |
| ------------------------- | ------------------------------------------------------------------------------------------ |
| 采集 unit 单调开始 / 结束 | 88,514,413,058 / 174,916,034,118 微秒                                                      |
| 独立单调经过时长          | 86,401.621060 秒，超过 24h，不使用 uptime 代替                                             |
| Gateway 起始单调时间      | 68,657,338 微秒，早于观察窗口；收尾前 PID 601、NRestarts=0                                 |
| 既有服务日志              | 窗口前 58 条与当前 boot、Invocation 匹配；首条 9 月 7 日 04:29:08.240Z，末条 17:08:26.255Z |
| 采集日志                  | 当前 boot 下 5 条，首尾单调时间 88,514,413,688 / 174,916,034,666 微秒                      |

结论：**24h 采样式低负载稳定观察经独立补证接受；v3 自动验收仍失败。**
这不是通过原自动门的认证报告，也不证明日志轮转完整或采样间从未短暂异常。
修复后采集器的自动认证仍需它自己的新运行证据；本轮不默认重复启动第三个 24h 窗口。

## 窗口外真实收发与单实例恢复

05:17 UTC 后，主 Agent 通过企业微信原生客户端，在已授权 Bot 私聊发送唯一标记
`LINUX-20260909-SOAK-END`，实际看到预期回复“长测后链路就绪”。显式指定 `--adapter=pi:rpc-v1`
的真实 ingress 校验匹配一条入站、九项通过；不是用 Bot 主动消息伪装人类入站。
该消息发生在观察窗口外，只证明收尾时实际链路可用。

本次 Channel 回执 447ms、Kernel 首事件 49ms、Kernel 首文本 6,673ms、总完成 7,403ms。
这是单次样本，不是延迟 SLA，也不据此调整 Agent 思考策略。结束后 ready=true，工作与
pending/leased/dead/spool 为 0，delivered=11。

随后先停止 Linux Gateway（确认 inactive、PID=0），再恢复 macOS 受管 Pi 单实例。
恢复后状态 healthy、无工作/投递积压或 dead。VM 正常关机，保留磁盘、两轮原报告与失败数据库。
切回后再以 `MAC-20260909-RESTORED` 进行真实私聊，客户端显示预期“日常链路已恢复”，
显式 Pi ingress 九项通过，delivered=684，pending/leased/dead/spool 为 0。
临时防休眠 job 已停止；专属 plist 移入系统废纸篓下 `wecom-soak-20260909`，可恢复，未改永久电源设置。

## 下一步仍围绕中间层

1. 未来采集器的静默日志判据已修复为 v4，加入同 boot/Invocation 日志锚点与缺失/矛盾回归；不重写本报告。
2. 可选停止卡终态后仍排队发送的生命周期问题已修复并有确定性回归；不扩大卡片主题，不影响默认文本/媒体路径。
3. 原生视频/引用回调与 Claude 自有认证仍单列；历史 Adapter 成功不冒充当前所有 Kernel 开箱即用。
   宿主物理断网、生产容量和跨主机多实例不由本轮关闭。

本轮完整 CI：41 个文件 / 423 项测试，格式、类型、公开检查通过。新故障测试不是新增真实客户端故障认证。
